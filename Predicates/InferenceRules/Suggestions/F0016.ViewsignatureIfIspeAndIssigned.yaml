id: F0016
code: F-032
name: ViewsignatureIfIspeAndIssigned
signature: 'IsPe(f) ∧ ¬IsSigned(f) → Suggest(ViewSignature(f))'
category: Suggestion/General
scope:
  object: file
  format: generic
description: Binary is unsigned. Review signature panel for certainty?
trigger_logic:
  antecedent:
  - IsPe(f)=true
  - ¬IsSigned(f)=true
  consequent:
  - Suggest(ViewSignature(f))
action:
  suggest: ViewSignature(f)
message: 'Binary is unsigned. Review signature panel for certainty?'
explanation: 'This suggestion accelerates investigation by opening the most relevant viewer for the detected indicators.'
cooldown: '30m'
tags:
  - pe
  - signing
severity:
  level: info
provenance:
  citations:
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
