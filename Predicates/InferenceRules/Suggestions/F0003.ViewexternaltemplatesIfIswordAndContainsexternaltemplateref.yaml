id: F0003
code: F-003
name: ViewexternaltemplatesIfIswordAndContainsexternaltemplateref
signature: 'IsWord(f) ∧ ContainsExternalTemplateRef(f) → Suggest(ViewExternalTemplates(f))'
category: Suggestion/Office
scope:
  object: file
  format: OOXML/OLE
description: Document references external template(s). Inspect RELs?
trigger_logic:
  antecedent:
  - IsWord(f)=true
  - ContainsExternalTemplateRef(f)=true
  consequent:
  - Suggest(ViewExternalTemplates(f))
action:
  suggest: ViewExternalTemplates(f)
message: 'Document references external template(s). Inspect RELs?'
explanation: 'This suggestion accelerates investigation by opening the most relevant viewer for the detected indicators.'
cooldown: '30m'
tags:
  - office
  - template
severity:
  level: warn
provenance:
  citations:
  - id: C004  # 'Template Injection (T1221)'
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
