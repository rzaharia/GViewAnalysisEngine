id: F0072
code: M-280
name: SafeextractIfIsarchiveAndArchiveispasswordprotected
signature: 'IsArchive(f) ∧ ArchiveIsPasswordProtected(f) ∧ ¬ExtractedArchiveSafely(f) → Suggest(SafeExtract(f))'
category: Suggestion/Archive
scope:
  object: file
  format: ZIP/RAR/7z
description: Password‑protected archive. Extract to sandbox?
trigger_logic:
  antecedent:
  - IsArchive(f)=true
  - ArchiveIsPasswordProtected(f)=true
  - ¬ExtractedArchiveSafely(f)=true
  consequent:
  - Suggest(SafeExtract(f))
action:
  suggest: SafeExtract(f)
message: 'Password‑protected archive. Extract to sandbox?'
explanation: 'Archives are frequently used to bypass filtering; reviewing members and extensions helps catch droppers and masquerading.'
cooldown: '30m'
tags: []
severity:
  level: warn
provenance:
  citations:
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
