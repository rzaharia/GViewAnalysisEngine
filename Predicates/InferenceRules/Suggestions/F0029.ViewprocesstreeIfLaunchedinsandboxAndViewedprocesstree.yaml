id: F0029
code: U-110
name: ViewprocesstreeIfLaunchedinsandboxAndViewedprocesstree
signature: 'LaunchedInSandbox(f) ∧ ¬ViewedProcessTree(f) → Suggest(ViewProcessTree(f))'
category: Suggestion/General
scope:
  object: file
  format: generic
description: Sandbox run active. Review process tree?
trigger_logic:
  antecedent:
  - LaunchedInSandbox(f)=true
  - ¬ViewedProcessTree(f)=true
  consequent:
  - Suggest(ViewProcessTree(f))
action:
  suggest: ViewProcessTree(f)
message: 'Sandbox run active. Review process tree?'
explanation: 'This suggestion accelerates investigation by opening the most relevant viewer for the detected indicators.'
cooldown: '15m'
tags:
  - dynamic
severity:
  level: warn
provenance:
  citations:
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
