id: F0080
code: P-300
name: ViewmacrosIfIswordAndHasmacros
signature: 'IsWord(f) ∧ HasMacros(f) ∧ UserEnabledMacros(u,f) ∧ ¬ViewedMacros(f) ∧ Within(5m) → Suggest(ViewMacros(f))'
category: Suggestion/Office
scope:
  object: file
  format: OOXML/OLE
description: Macros executed recently. Inspect source now.
trigger_logic:
  antecedent:
  - IsWord(f)=true
  - HasMacros(f)=true
  - UserEnabledMacros(u,f)=true
  - ¬ViewedMacros(f)=true
  - Within(5m)=true
  consequent:
  - Suggest(ViewMacros(f))
action:
  suggest: ViewMacros(f)
message: 'Macros executed recently. Inspect source now.'
explanation: 'Office macros are a common initial execution vector; reviewing macro content improves triage and reveals downloader or persistence code.'
cooldown: '15m'
tags: []
severity:
  level: high
provenance:
  citations:
  - id: C001  # 'Invoice #31415 attached: Automated analysis of malicious Microsoft Office documents'
  - id: C045  # 'User Execution: Malicious File (T1204.002)'
  - id: C009  # 'Cheat Sheet for Analyzing Malicious Documents'
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
