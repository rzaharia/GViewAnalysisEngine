id: F0018
code: F-040
name: SafeextractIfIsarchiveAndArchiveispasswordprotected
signature: 'IsArchive(f) ∧ ArchiveIsPasswordProtected(f) → Suggest(SafeExtract(f))'
category: Suggestion/Archive
scope:
  object: file
  format: ZIP/RAR/7z
description: Password‑protected archive. Safe‑extract to sandbox?
trigger_logic:
  antecedent:
  - IsArchive(f)=true
  - ArchiveIsPasswordProtected(f)=true
  consequent:
  - Suggest(SafeExtract(f))
action:
  suggest: SafeExtract(f)
message: 'Password‑protected archive. Safe‑extract to sandbox?'
explanation: 'Archives are frequently used to bypass filtering; reviewing members and extensions helps catch droppers and masquerading.'
cooldown: '30m'
tags:
  - archive
  - safety
severity:
  level: warn
provenance:
  citations:
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
