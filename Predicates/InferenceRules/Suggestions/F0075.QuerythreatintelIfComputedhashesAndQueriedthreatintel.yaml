id: F0075
code: M-290
name: QuerythreatintelIfComputedhashesAndQueriedthreatintel
signature: 'ComputedHashes(f) ∧ ¬QueriedThreatIntel(f, any) ∧ (ContainsUrl(f) ∨ TriesToAccessTheInternet(f)) → Suggest(QueryThreatIntel(f))'
category: Suggestion/General
scope:
  object: file
  format: generic
description: Reputation check likely useful here. Query TI?
trigger_logic:
  antecedent:
  - ComputedHashes(f)=true
  - ¬QueriedThreatIntel(f, any)=true
  - (ContainsUrl(f) ∨ TriesToAccessTheInternet(f))=true
  consequent:
  - Suggest(QueryThreatIntel(f))
action:
  suggest: QueryThreatIntel(f)
message: 'Reputation check likely useful here. Query TI?'
explanation: 'This suggestion accelerates investigation by opening the most relevant viewer for the detected indicators.'
cooldown: '30m'
tags: []
severity:
  level: info
provenance:
  citations:
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
