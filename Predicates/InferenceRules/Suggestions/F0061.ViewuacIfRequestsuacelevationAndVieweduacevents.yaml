id: F0061
code: M-241
name: ViewuacIfRequestsuacelevationAndVieweduacevents
signature: 'RequestsUacElevation(f) ∧ ¬ViewedUacEvents(f) → Suggest(ViewUac(f))'
category: Suggestion/General
scope:
  object: file
  format: generic
description: UAC prompt/elevation attempt. Inspect event details?
trigger_logic:
  antecedent:
  - RequestsUacElevation(f)=true
  - ¬ViewedUacEvents(f)=true
  consequent:
  - Suggest(ViewUac(f))
action:
  suggest: ViewUac(f)
message: 'UAC prompt/elevation attempt. Inspect event details?'
explanation: 'This suggestion accelerates investigation by opening the most relevant viewer for the detected indicators.'
cooldown: '30m'
tags: []
severity:
  level: warn
provenance:
  citations:
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
