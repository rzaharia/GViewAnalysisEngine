id: F0092
code: E-700
name: VieworiginIfSourceisemailattachmentAndHassuspiciousattachment
signature: 'SourceIsEmailAttachment(f) ∧ HasSuspiciousAttachment(f) ∧ ¬ViewedOrigin(f) → Suggest(ViewOrigin(f))'
category: Suggestion/General
scope:
  object: file
  format: generic
description: Check message headers and sender context.
trigger_logic:
  antecedent:
  - SourceIsEmailAttachment(f)=true
  - HasSuspiciousAttachment(f)=true
  - ¬ViewedOrigin(f)=true
  consequent:
  - Suggest(ViewOrigin(f))
action:
  suggest: ViewOrigin(f)
message: 'Check message headers and sender context.'
explanation: 'This suggestion accelerates investigation by opening the most relevant viewer for the detected indicators.'
cooldown: '45m'
tags: []
severity:
  level: warn
provenance:
  citations:
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
