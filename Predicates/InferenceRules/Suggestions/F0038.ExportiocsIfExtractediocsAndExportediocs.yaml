id: F0038
code: U-132
name: ExportiocsIfExtractediocsAndExportediocs
signature: 'ExtractedIOCs(f) ∧ ¬ExportedIOCs(f, any) → Suggest(ExportIOCs(f))'
category: Suggestion/General
scope:
  object: file
  format: generic
description: Export IOCs (CSV/STIX) for sharing?
trigger_logic:
  antecedent:
  - ExtractedIOCs(f)=true
  - ¬ExportedIOCs(f, any)=true
  consequent:
  - Suggest(ExportIOCs(f))
action:
  suggest: ExportIOCs(f)
message: 'Export IOCs (CSV/STIX) for sharing?'
explanation: 'This suggestion accelerates investigation by opening the most relevant viewer for the detected indicators.'
cooldown: '1h'
tags:
  - ioc
severity:
  level: info
provenance:
  citations:
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
