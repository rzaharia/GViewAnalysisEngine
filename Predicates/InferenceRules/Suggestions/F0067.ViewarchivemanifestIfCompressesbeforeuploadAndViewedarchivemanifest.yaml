id: F0067
code: M-261
name: ViewarchivemanifestIfCompressesbeforeuploadAndViewedarchivemanifest
signature: 'CompressesBeforeUpload(f) ∧ ¬ViewedArchiveManifest(f) → Suggest(ViewArchiveManifest(f))'
category: Suggestion/Archive
scope:
  object: file
  format: ZIP/RAR/7z
description: Local staging via archive. Inspect staged files?
trigger_logic:
  antecedent:
  - CompressesBeforeUpload(f)=true
  - ¬ViewedArchiveManifest(f)=true
  consequent:
  - Suggest(ViewArchiveManifest(f))
action:
  suggest: ViewArchiveManifest(f)
message: 'Local staging via archive. Inspect staged files?'
explanation: 'Archives are frequently used to bypass filtering; reviewing members and extensions helps catch droppers and masquerading.'
cooldown: '20m'
tags: []
severity:
  level: warn
provenance:
  citations:
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
