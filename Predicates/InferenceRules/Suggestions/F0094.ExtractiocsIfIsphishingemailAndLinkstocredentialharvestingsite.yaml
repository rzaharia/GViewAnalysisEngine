id: F0094
code: E-702
name: ExtractiocsIfIsphishingemailAndLinkstocredentialharvestingsite
signature: 'IsPhishingEmail(f) ∧ LinksToCredentialHarvestingSite(f) ∧ ¬ExtractedIOCs(f) → Suggest(ExtractIOCs(f))'
category: Suggestion/General
scope:
  object: file
  format: generic
description: Extract URLs/domains for blocking.
trigger_logic:
  antecedent:
  - IsPhishingEmail(f)=true
  - LinksToCredentialHarvestingSite(f)=true
  - ¬ExtractedIOCs(f)=true
  consequent:
  - Suggest(ExtractIOCs(f))
action:
  suggest: ExtractIOCs(f)
message: 'Extract URLs/domains for blocking.'
explanation: 'This suggestion accelerates investigation by opening the most relevant viewer for the detected indicators.'
cooldown: '45m'
tags: []
severity:
  level: high
provenance:
  citations:
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
