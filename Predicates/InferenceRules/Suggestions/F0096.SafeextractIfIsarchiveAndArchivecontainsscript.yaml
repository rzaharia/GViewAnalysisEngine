id: F0096
code: A-801
name: SafeextractIfIsarchiveAndArchivecontainsscript
signature: 'IsArchive(f) ∧ ArchiveContainsScript(f) ∧ ¬ExtractedArchiveSafely(f) → Suggest(SafeExtract(f))'
category: Suggestion/Scripts
scope:
  object: file
  format: Script
description: Scripts inside. Safe‑extract to sandbox.
trigger_logic:
  antecedent:
  - IsArchive(f)=true
  - ArchiveContainsScript(f)=true
  - ¬ExtractedArchiveSafely(f)=true
  consequent:
  - Suggest(SafeExtract(f))
action:
  suggest: SafeExtract(f)
message: 'Scripts inside. Safe‑extract to sandbox.'
explanation: 'Deobfuscation and dynamic invocation tracing expose hidden URLs, loaders, and privilege escalation steps.'
cooldown: '45m'
tags: []
severity:
  level: warn
provenance:
  citations:
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
