id: I0153
name: CreatesrunkeyTriestoaccesstheinternetToIssuspiciousFinalHigh
signature: CreatesRunKey(f) ∧ TriesToAccessTheInternet(f) ∧ ¬UserInteractionWithin(10m)
  → IsSuspicious(f) {Final, high}
category: Behavioral/TimeWindowed
scope:
  object: file
  postconditions:
  - Flag: IsSuspicious(f) {Final, high}
description: When CreatesRunKey(f) ∧ TriesToAccessTheInternet(f) ∧ ¬UserInteractionWithin(10m)
  holds, assert IsSuspicious(f) {Final, high}.
trigger_logic:
  antecedent:
  - CreatesRunKey(f)=true
  - TriesToAccessTheInternet(f)=true
  - ¬UserInteractionWithin(10m)=true
  consequent:
  - IsSuspicious(f) {Final, high}=true
evidence_binding:
  requires:
  - artifact: timeline
    fields:
    - delta
    - sequence
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if Endpoint not in allowlist and SNI mismatch: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations:
  - id: C049
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
