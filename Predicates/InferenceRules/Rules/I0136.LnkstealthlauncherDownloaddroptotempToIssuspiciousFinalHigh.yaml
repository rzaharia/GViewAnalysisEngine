id: I0136
name: LnkstealthlauncherDownloaddroptotempToIssuspiciousFinalHigh
signature: LnkStealthLauncher(f) ∧ DownloadDropToTemp(f) → IsSuspicious(f) {Final,
  high}
category: Behavioral/LnkAbuse
scope:
  object: file
  postconditions:
  - Flag: IsSuspicious(f) {Final, high}
description: When LnkStealthLauncher(f) ∧ DownloadDropToTemp(f) holds, assert IsSuspicious(f)
  {Final, high}.
trigger_logic:
  antecedent:
  - LnkStealthLauncher(f)=true
  - DownloadDropToTemp(f)=true
  consequent:
  - IsSuspicious(f) {Final, high}=true
evidence_binding:
  requires:
  - artifact: lnk
    fields:
    - target
    - args
    - icon
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if corroborated by ≥2 telemetry sources: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations:
  - id: C014
  - id: C015
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
