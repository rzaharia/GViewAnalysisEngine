id: I0043
name: ScriptdownloaderDownloadsfrominternetToIsmaliciousFinalHigh
signature: ScriptDownloader(f) ∧ DownloadsFromInternet(f) ∧ DropsAndExecutes(f) →
  IsMalicious(f) {Final, high}
category: Behavioral/Scripts
scope:
  object: file
  postconditions:
  - Flag: IsMalicious(f) {Final, high}
description: When ScriptDownloader(f) ∧ DownloadsFromInternet(f) ∧ DropsAndExecutes(f)
  holds, assert IsMalicious(f) {Final, high}.
trigger_logic:
  antecedent:
  - ScriptDownloader(f)=true
  - DownloadsFromInternet(f)=true
  - DropsAndExecutes(f)=true
  consequent:
  - IsMalicious(f) {Final, high}=true
evidence_binding:
  requires:
  - artifact: script_source
    fields:
    - suspicious_keywords
    - urls
    - base64_blobs
  - artifact: process_tree
    fields:
    - interpreter
    - cmdline
  - artifact: netflow
    fields:
    - dst_domain
    - url
    - bytes_out
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if Endpoint not in allowlist and SNI mismatch: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations:
  - id: C038
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
