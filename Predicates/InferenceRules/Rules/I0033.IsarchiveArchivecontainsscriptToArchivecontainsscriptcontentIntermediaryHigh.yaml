id: I0033
name: IsarchiveArchivecontainsscriptToArchivecontainsscriptcontentIntermediaryHigh
signature: IsArchive(f) ∧ ArchiveContainsScript(f) → ArchiveContainsScriptContent(f)
  {Intermediary, high}
category: Behavioral/ArchiveContainer
scope:
  object: file
  postconditions:
  - Derive: ArchiveContainsScriptContent(f) {Intermediary, high}
description: When IsArchive(f) ∧ ArchiveContainsScript(f) holds, assert ArchiveContainsScriptContent(f)
  {Intermediary, high}.
trigger_logic:
  antecedent:
  - IsArchive(f)=true
  - ArchiveContainsScript(f)=true
  consequent:
  - ArchiveContainsScriptContent(f) {Intermediary, high}=true
evidence_binding:
  requires:
  - artifact: archive_manifest
    fields:
    - members
    - password_protected
    - double_extensions
  - artifact: email_headers
    fields:
    - from
    - subject
    - attachments
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if corroborated by ≥2 telemetry sources: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations:
  - id: C012
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
