id: I0133
name: InstallerdownloadsorscriptsPersistenceachievedToIssuspiciousFinalHigh
signature: InstallerDownloadsOrScripts(f) ∧ PersistenceAchieved(f) → IsSuspicious(f)
  {Final, high}
category: Behavioral/InstallerMsi
scope:
  object: file
  postconditions:
  - Flag: IsSuspicious(f) {Final, high}
description: When InstallerDownloadsOrScripts(f) ∧ PersistenceAchieved(f) holds, assert
  IsSuspicious(f) {Final, high}.
trigger_logic:
  antecedent:
  - InstallerDownloadsOrScripts(f)=true
  - PersistenceAchieved(f)=true
  consequent:
  - IsSuspicious(f) {Final, high}=true
evidence_binding:
  requires:
  - artifact: msi_custom_actions
    fields:
    - action
    - path
  - artifact: netflow
    fields:
    - url
    - bytes_out
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if corroborated by ≥2 telemetry sources: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations:
  - id: C051
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
