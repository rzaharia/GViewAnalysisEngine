id: I0116
name: InternetdeliveredattachmentToIssuspiciousFinalHigh
signature: InternetDeliveredAttachment(f) ∧ (MacroAutoExecCapable(f) ∨ ScriptDownloader(f))
  → IsSuspicious(f) {Final, high}
category: Behavioral/Reputation
scope:
  object: file
  postconditions:
  - Flag: IsSuspicious(f) {Final, high}
description: When InternetDeliveredAttachment(f) ∧ (MacroAutoExecCapable(f) ∨ ScriptDownloader(f))
  holds, assert IsSuspicious(f) {Final, high}.
trigger_logic:
  antecedent:
  - InternetDeliveredAttachment(f)=true
  - (MacroAutoExecCapable(f) ∨ ScriptDownloader(f))=true
  consequent:
  - IsSuspicious(f) {Final, high}=true
evidence_binding:
  requires:
  - artifact: mark_of_web
    fields:
    - zone
    - referrer
  - artifact: signature
    fields:
    - subject
    - chain_status
  - artifact: threat_intel
    fields:
    - feeds
    - first_seen
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if corroborated by ≥2 telemetry sources: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations:
  - id: C039
  - id: C045
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
