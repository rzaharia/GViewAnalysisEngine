id: I0128
name: CreatessystemduserserviceModifiessudoersToLinuxelevatedpersistenceIntermediaryHi
signature: CreatesSystemdUserService(f) ∨ ModifiesSudoers(f) ∨ CreatesSetuidBinary(f)
  → LinuxElevatedPersistence(f) {Intermediary, high}
category: Behavioral/OsSpecific
scope:
  object: file
  postconditions:
  - Derive: LinuxElevatedPersistence(f) {Intermediary, high}
description: When CreatesSystemdUserService(f) ∨ ModifiesSudoers(f) ∨ CreatesSetuidBinary(f)
  holds, assert LinuxElevatedPersistence(f) {Intermediary, high}.
trigger_logic:
  antecedent:
  - CreatesSystemdUserService(f) ∨ ModifiesSudoers(f) ∨ CreatesSetuidBinary(f)=true
  consequent:
  - LinuxElevatedPersistence(f) {Intermediary, high}=true
evidence_binding:
  requires:
  - artifact: launchd
    fields:
    - plist
    - agent_or_daemon
  - artifact: systemd
    fields:
    - unit_name
    - user_scope
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if corroborated by ≥2 telemetry sources: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations: []
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
