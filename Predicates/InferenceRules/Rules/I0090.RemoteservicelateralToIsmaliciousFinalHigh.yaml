id: I0090
name: RemoteservicelateralToIsmaliciousFinalHigh
signature: (WmiSmbLateralMovement(f) ∨ RemoteServiceLateral(f) ∨ PsExecStyleMovement(f))
  ∧ CredentialAccessAttempt(f) → IsMalicious(f) {Final, high}
category: Behavioral/LateralMovement
scope:
  object: file
  postconditions:
  - Flag: IsMalicious(f) {Final, high}
description: When (WmiSmbLateralMovement(f) ∨ RemoteServiceLateral(f) ∨ PsExecStyleMovement(f))
  ∧ CredentialAccessAttempt(f) holds, assert IsMalicious(f) {Final, high}.
trigger_logic:
  antecedent:
  - (WmiSmbLateralMovement(f) ∨ RemoteServiceLateral(f) ∨ PsExecStyleMovement(f))=true
  - CredentialAccessAttempt(f)=true
  consequent:
  - IsMalicious(f) {Final, high}=true
evidence_binding:
  requires:
  - artifact: smb_logs
    fields:
    - share
    - ip
    - auth_user
  - artifact: winrm_logs
    fields:
    - host
    - user
  - artifact: service_control
    fields:
    - sc_action
    - service_name
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if corroborated by ≥2 telemetry sources: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations:
  - id: C050
  - id: C051
  - id: C052
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
