id: I0088
name: UseswinrmToAdminremoteopspatternIntermediaryMed
signature: UsesWinRm(f) ∧ (EnumeratesAd(f) ∨ ScansNetwork(f)) → AdminRemoteOpsPattern(f)
  {Intermediary, med}
category: Behavioral/LateralMovement
scope:
  object: file
  postconditions:
  - Derive: AdminRemoteOpsPattern(f) {Intermediary, med}
description: When UsesWinRm(f) ∧ (EnumeratesAd(f) ∨ ScansNetwork(f)) holds, assert
  AdminRemoteOpsPattern(f) {Intermediary, med}.
trigger_logic:
  antecedent:
  - UsesWinRm(f)=true
  - (EnumeratesAd(f) ∨ ScansNetwork(f))=true
  consequent:
  - AdminRemoteOpsPattern(f) {Intermediary, med}=true
evidence_binding:
  requires:
  - artifact: smb_logs
    fields:
    - share
    - ip
    - auth_user
  - artifact: winrm_logs
    fields:
    - host
    - user
  - artifact: service_control
    fields:
    - sc_action
    - service_name
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if corroborated by ≥2 telemetry sources: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations:
  - id: C053
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
