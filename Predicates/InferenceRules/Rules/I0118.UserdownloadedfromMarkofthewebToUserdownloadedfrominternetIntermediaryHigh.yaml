id: I0118
name: UserdownloadedfromMarkofthewebToUserdownloadedfrominternetIntermediaryHigh
signature: UserDownloadedFrom(u,f,src) ∧ MarkOfTheWeb(f) → UserDownloadedFromInternet(f)
  {Intermediary, high}
category: Behavioral/UserCoupling
scope:
  object: file
  postconditions:
  - Derive: UserDownloadedFromInternet(f) {Intermediary, high}
description: When UserDownloadedFrom(u,f,src) ∧ MarkOfTheWeb(f) holds, assert UserDownloadedFromInternet(f)
  {Intermediary, high}.
trigger_logic:
  antecedent:
  - UserDownloadedFrom(u,f,src)=true
  - MarkOfTheWeb(f)=true
  consequent:
  - UserDownloadedFromInternet(f) {Intermediary, high}=true
evidence_binding:
  requires:
  - artifact: ui_audit
    fields:
    - user_action
    - timestamp
  - artifact: mark_of_web
    fields:
    - zone
    - referrer
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if corroborated by ≥2 telemetry sources: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations:
  - id: C045
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
