id: I0038
name: IsscriptContainsdownloadercodeToScriptdownloaderIntermediaryHigh
signature: IsScript(f) ∧ ContainsDownloaderCode(f) → ScriptDownloader(f) {Intermediary,
  high}
category: Behavioral/Scripts
scope:
  object: file
  postconditions:
  - Derive: ScriptDownloader(f) {Intermediary, high}
description: When IsScript(f) ∧ ContainsDownloaderCode(f) holds, assert ScriptDownloader(f)
  {Intermediary, high}.
trigger_logic:
  antecedent:
  - IsScript(f)=true
  - ContainsDownloaderCode(f)=true
  consequent:
  - ScriptDownloader(f) {Intermediary, high}=true
evidence_binding:
  requires:
  - artifact: script_source
    fields:
    - suspicious_keywords
    - urls
    - base64_blobs
  - artifact: process_tree
    fields:
    - interpreter
    - cmdline
  - artifact: netflow
    fields:
    - dst_domain
    - url
    - bytes_out
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if corroborated by ≥2 telemetry sources: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations:
  - id: C038
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
