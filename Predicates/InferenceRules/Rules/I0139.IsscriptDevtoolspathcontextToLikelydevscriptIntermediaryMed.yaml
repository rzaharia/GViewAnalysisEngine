id: I0139
name: IsscriptDevtoolspathcontextToLikelydevscriptIntermediaryMed
signature: IsScript(f) ∧ DevToolsPathContext(f) ∧ ¬(TriesToAccessTheInternet(f) ∨
  DropsAndExecutes(f)) → LikelyDevScript(f) {Intermediary, med}
category: Behavioral/BenignProfiling
scope:
  object: file
  postconditions:
  - Derive: LikelyDevScript(f) {Intermediary, med}
description: When IsScript(f) ∧ DevToolsPathContext(f) ∧ ¬(TriesToAccessTheInternet(f)
  ∨ DropsAndExecutes(f)) holds, assert LikelyDevScript(f) {Intermediary, med}.
trigger_logic:
  antecedent:
  - IsScript(f)=true
  - DevToolsPathContext(f)=true
  - ¬(TriesToAccessTheInternet(f) ∨ DropsAndExecutes(f))=true
  consequent:
  - LikelyDevScript(f) {Intermediary, med}=true
evidence_binding:
  requires:
  - artifact: usage_stats
    fields:
    - frequency
    - publisher
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if Endpoint not in allowlist and SNI mismatch: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations: []
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
