id: I0053
name: InjectionbehaviorBeaconingpatternToIssuspiciousFinalHigh
signature: InjectionBehavior(f) ∧ BeaconingPattern(f) → IsSuspicious(f) {Final, high}
category: Behavioral/PEBinary
scope:
  object: file
  postconditions:
  - Flag: IsSuspicious(f) {Final, high}
description: When InjectionBehavior(f) ∧ BeaconingPattern(f) holds, assert IsSuspicious(f)
  {Final, high}.
trigger_logic:
  antecedent:
  - InjectionBehavior(f)=true
  - BeaconingPattern(f)=true
  consequent:
  - IsSuspicious(f) {Final, high}=true
evidence_binding:
  requires:
  - artifact: pe_static
    fields:
    - imports
    - sections
    - entropy
  - artifact: process_tree
    fields:
    - parent
    - children
    - injection_calls
  - artifact: fs_events
    fields:
    - writes
    - drops
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if Periodicity jitter < 10%: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses:
  - Direct syscalls to evade hooks
  - Signed vulnerable driver (BYOVD)
  mitigations:
  - Kernel ETW for syscalls
  - Blocklist vulnerable drivers
provenance:
  citations:
  - id: C036
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: medium
version: 1
