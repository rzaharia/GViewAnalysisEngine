id: I0085
name: MassencryptionsuspectedBeaconingpatternToIssuspiciousFinalHigh
signature: MassEncryptionSuspected(f) ∧ BeaconingPattern(f) → IsSuspicious(f) {Final,
  high}
category: Behavioral/Ransomware
scope:
  object: file
  postconditions:
  - Flag: IsSuspicious(f) {Final, high}
description: When MassEncryptionSuspected(f) ∧ BeaconingPattern(f) holds, assert IsSuspicious(f)
  {Final, high}.
trigger_logic:
  antecedent:
  - MassEncryptionSuspected(f)=true
  - BeaconingPattern(f)=true
  consequent:
  - IsSuspicious(f) {Final, high}=true
evidence_binding:
  requires:
  - artifact: fs_events
    fields:
    - modified_files
    - entropy_delta
    - extensions_changed
  - artifact: vssadmin_events
    fields:
    - operation
    - result
  - artifact: services
    fields:
    - name
    - action
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if Periodicity jitter < 10%: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses:
  - Sparse encryption
  - Selective VSS deletions
  mitigations:
  - FS honeypots
  - VSS event alerting
provenance:
  citations:
  - id: C036
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
