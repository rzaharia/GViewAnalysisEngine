id: I0103
name: CovertexecutionpatternSecuritytoolinterferenceToIsmaliciousFinalHigh
signature: CovertExecutionPattern(f) ∧ SecurityToolInterference(f) → IsMalicious(f)
  {Final, high}
category: Behavioral/AntiAnalysis
scope:
  object: file
  postconditions:
  - Flag: IsMalicious(f) {Final, high}
description: When CovertExecutionPattern(f) ∧ SecurityToolInterference(f) holds, assert
  IsMalicious(f) {Final, high}.
trigger_logic:
  antecedent:
  - CovertExecutionPattern(f)=true
  - SecurityToolInterference(f)=true
  consequent:
  - IsMalicious(f) {Final, high}=true
evidence_binding:
  requires:
  - artifact: anti_vm
    fields:
    - registry_keys
    - drivers
    - timing
  - artifact: process_tree
    fields:
    - ppid_spoofing
    - unhooks
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if corroborated by ≥2 telemetry sources: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations:
  - id: C063
  - id: C024
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
