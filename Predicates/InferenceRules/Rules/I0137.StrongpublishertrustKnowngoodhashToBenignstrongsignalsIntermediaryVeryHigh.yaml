id: I0137
name: StrongpublishertrustKnowngoodhashToBenignstrongsignalsIntermediaryVeryHigh
signature: StrongPublisherTrust(f) ∨ KnownGoodHash(f) → BenignStrongSignals(f) {Intermediary,
  very-high}
category: Behavioral/BenignProfiling
scope:
  object: file
  postconditions:
  - Derive: BenignStrongSignals(f) {Intermediary, very-high}
description: When StrongPublisherTrust(f) ∨ KnownGoodHash(f) holds, assert BenignStrongSignals(f)
  {Intermediary, very-high}.
trigger_logic:
  antecedent:
  - StrongPublisherTrust(f) ∨ KnownGoodHash(f)=true
  consequent:
  - BenignStrongSignals(f) {Intermediary, very-high}=true
evidence_binding:
  requires:
  - artifact: usage_stats
    fields:
    - frequency
    - publisher
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if corroborated by ≥2 telemetry sources: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations: []
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
