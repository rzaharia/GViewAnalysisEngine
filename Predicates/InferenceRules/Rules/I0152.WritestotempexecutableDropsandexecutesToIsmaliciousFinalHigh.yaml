id: I0152
name: WritestotempexecutableDropsandexecutesToIsmaliciousFinalHigh
signature: WritesToTempExecutable(f) ∧ DropsAndExecutes(f) ∧ BeaconingPattern(f) ∧
  Within(2m) → IsMalicious(f) {Final, high}
category: Behavioral/TimeWindowed
scope:
  object: file
  postconditions:
  - Flag: IsMalicious(f) {Final, high}
description: When WritesToTempExecutable(f) ∧ DropsAndExecutes(f) ∧ BeaconingPattern(f)
  ∧ Within(2m) holds, assert IsMalicious(f) {Final, high}.
trigger_logic:
  antecedent:
  - WritesToTempExecutable(f)=true
  - DropsAndExecutes(f)=true
  - BeaconingPattern(f)=true
  - Within(2m)=true
  consequent:
  - IsMalicious(f) {Final, high}=true
evidence_binding:
  requires:
  - artifact: timeline
    fields:
    - delta
    - sequence
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if Periodicity jitter < 10%: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations:
  - id: C036
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
