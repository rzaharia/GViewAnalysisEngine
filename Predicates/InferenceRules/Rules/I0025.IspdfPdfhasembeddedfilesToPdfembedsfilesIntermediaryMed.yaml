id: I0025
name: IspdfPdfhasembeddedfilesToPdfembedsfilesIntermediaryMed
signature: IsPdf(f) ∧ PdfHasEmbeddedFiles(f) → PdfEmbedsFiles(f) {Intermediary, med}
category: Behavioral/PdfRtfOle
scope:
  object: file
  postconditions:
  - Derive: PdfEmbedsFiles(f) {Intermediary, med}
description: When IsPdf(f) ∧ PdfHasEmbeddedFiles(f) holds, assert PdfEmbedsFiles(f)
  {Intermediary, med}.
trigger_logic:
  antecedent:
  - IsPdf(f)=true
  - PdfHasEmbeddedFiles(f)=true
  consequent:
  - PdfEmbedsFiles(f) {Intermediary, med}=true
evidence_binding:
  requires:
  - artifact: pdf_objects
    fields:
    - OpenAction
    - Launch
    - JS_count
  - artifact: process_tree
    fields:
    - parent_pid
    - cmdline
  - artifact: netflow
    fields:
    - dst_ip
    - dst_domain
    - url
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if corroborated by ≥2 telemetry sources: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations:
  - id: C009
  - id: C045
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
