id: I0122
name: ConfirmedelevationPersistenceachievedToIsmaliciousFinalHigh
signature: ConfirmedElevation(f) ∧ PersistenceAchieved(f) → IsMalicious(f) {Final,
  high}
category: Behavioral/UserCoupling
scope:
  object: file
  postconditions:
  - Flag: IsMalicious(f) {Final, high}
description: When ConfirmedElevation(f) ∧ PersistenceAchieved(f) holds, assert IsMalicious(f)
  {Final, high}.
trigger_logic:
  antecedent:
  - ConfirmedElevation(f)=true
  - PersistenceAchieved(f)=true
  consequent:
  - IsMalicious(f) {Final, high}=true
evidence_binding:
  requires:
  - artifact: ui_audit
    fields:
    - user_action
    - timestamp
  - artifact: mark_of_web
    fields:
    - zone
    - referrer
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if corroborated by ≥2 telemetry sources: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations:
  - id: C045
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
