id: I0077
name: ReadslsassmemoryToCredentialdumpexfilpatternIntermediaryHigh
signature: ReadsLsassMemory(f) ∧ (UploadsLargeVolume(f) ∨ CompressesBeforeUpload(f))
  → CredentialDumpExfilPattern(f) {Intermediary, high}
category: Behavioral/PrivilegeCreds
scope:
  object: file
  postconditions:
  - Derive: CredentialDumpExfilPattern(f) {Intermediary, high}
description: When ReadsLsassMemory(f) ∧ (UploadsLargeVolume(f) ∨ CompressesBeforeUpload(f))
  holds, assert CredentialDumpExfilPattern(f) {Intermediary, high}.
trigger_logic:
  antecedent:
  - ReadsLsassMemory(f)=true
  - (UploadsLargeVolume(f) ∨ CompressesBeforeUpload(f))=true
  consequent:
  - CredentialDumpExfilPattern(f) {Intermediary, high}=true
evidence_binding:
  requires:
  - artifact: security_event_logs
    fields:
    - event_id
    - subject_user
    - target_process
  - artifact: process_tree
    fields:
    - privileges
    - reads_lsass
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if corroborated by ≥2 telemetry sources: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations:
  - id: C032
  - id: C048
  - id: C054
  - id: C043
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
