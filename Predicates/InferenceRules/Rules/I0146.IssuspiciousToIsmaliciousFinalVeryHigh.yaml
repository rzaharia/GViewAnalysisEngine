id: I0146
name: IssuspiciousToIsmaliciousFinalVeryHigh
signature: IsSuspicious(f) ∧ (KnownBadHash(f) ∨ HashInThreatIntel(f)) → IsMalicious(f)
  {Final, very-high}
category: Behavioral/GlobalAggregation
scope:
  object: file
  postconditions:
  - Flag: IsMalicious(f) {Final, very-high}
description: When IsSuspicious(f) ∧ (KnownBadHash(f) ∨ HashInThreatIntel(f)) holds,
  assert IsMalicious(f) {Final, very-high}.
trigger_logic:
  antecedent:
  - IsSuspicious(f)=true
  - (KnownBadHash(f) ∨ HashInThreatIntel(f))=true
  consequent:
  - IsMalicious(f) {Final, very-high}=true
evidence_binding:
  requires:
  - artifact: summary
    fields:
    - suspicion_sources
    - malicious_sources
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if corroborated by ≥2 telemetry sources: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations:
  - id: C036
  - id: C038
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: high
version: 1
