id: I0155
name: LikelybenignToRecheckwithsandboxFinalMed
signature: LikelyBenign(f) ∧ (MarkOfTheWeb(f) ∨ InternetDeliveredAttachment(f)) →
  RecheckWithSandbox(f) {Final, med}
category: Behavioral/Prioritization
scope:
  object: file
  postconditions:
  - Derive: RecheckWithSandbox(f) {Final, med}
description: When LikelyBenign(f) ∧ (MarkOfTheWeb(f) ∨ InternetDeliveredAttachment(f))
  holds, assert RecheckWithSandbox(f) {Final, med}.
trigger_logic:
  antecedent:
  - LikelyBenign(f)=true
  - (MarkOfTheWeb(f) ∨ InternetDeliveredAttachment(f))=true
  consequent:
  - RecheckWithSandbox(f) {Final, med}=true
evidence_binding:
  requires:
  - artifact: suggestions
    fields:
    - pending
    - priority
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if corroborated by ≥2 telemetry sources: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations:
  - id: C009
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
