id: I0079
name: CredentialaccessattemptBeaconingpatternToIsmaliciousFinalHigh
signature: CredentialAccessAttempt(f) ∧ BeaconingPattern(f) → IsMalicious(f) {Final,
  high}
category: Behavioral/PrivilegeCreds
scope:
  object: file
  postconditions:
  - Flag: IsMalicious(f) {Final, high}
description: When CredentialAccessAttempt(f) ∧ BeaconingPattern(f) holds, assert IsMalicious(f)
  {Final, high}.
trigger_logic:
  antecedent:
  - CredentialAccessAttempt(f)=true
  - BeaconingPattern(f)=true
  consequent:
  - IsMalicious(f) {Final, high}=true
evidence_binding:
  requires:
  - artifact: security_event_logs
    fields:
    - event_id
    - subject_user
    - target_process
  - artifact: process_tree
    fields:
    - privileges
    - reads_lsass
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if Periodicity jitter < 10%: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations:
  - id: C036
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: low
version: 1
