id: I0126
name: Clusteredc2campaignClusteredpersistencecampaignToIsmaliciouscampaignFinalHigh
signature: ClusteredC2Campaign(f1,f2) ∨ ClusteredPersistenceCampaign(f1,f2) → IsMaliciousCampaign()
  {Final, high}
category: Behavioral/CampaignCorrelation
scope:
  object: file
  postconditions:
  - Flag: IsMaliciousCampaign() {Final, high}
description: When ClusteredC2Campaign(f1,f2) ∨ ClusteredPersistenceCampaign(f1,f2)
  holds, assert IsMaliciousCampaign() {Final, high}.
trigger_logic:
  antecedent:
  - ClusteredC2Campaign(f1,f2) ∨ ClusteredPersistenceCampaign(f1,f2)=true
  consequent:
  - IsMaliciousCampaign() {Final, high}=true
evidence_binding:
  requires:
  - artifact: cluster
    fields:
    - c2_family
    - overlap_indicators
  retention_hint: Store ±120s around key triggering events.
confidence:
  base: 0.6
  modifiers:
  - 'if corroborated by ≥2 telemetry sources: +0.05'
  calibration: isotonic_2025_09
severity:
  level: info
  rationale: Derived from rule tier and observed behaviors.
conflict_handling:
  contradicts:
  - 'Rule: R-1980 ''BenignAutoUpdate'''
  resolution: Prefer higher confidence unless allowlist(domain or signer) applies.
adversary_evasion:
  known_bypasses: []
  mitigations: []
provenance:
  citations: []
  author: '@gview-team'
implementation:
  status: planned
  perf_cost: high
version: 1
